import React from 'react'


const Child = (props) => {
    console.log(props)

    return (
        <div>
            {/* <p>name: {props.name}</p>
            <p>age: {props.age}</p>
            <p>city: {props.city}</p> */}
        </div>
    )
}

export default Child;